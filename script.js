const myName = "Mohammad";
const myFamily = "Rezaii";

// this function logs the user's full name
function logMyName(name, family) {
	console.log(`my name is ${name} ${family}`);
}

logMyName(myName, myFamily);
